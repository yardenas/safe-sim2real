# @package _global_
defaults:
  - go1_joystick
  - _self_
  
environment:
  task_name: SafeGo1JoystickFlatTerrain

training:
  num_envs: 32
  train_domain_randomization: true
  eval_domain_randomization: true
  safe: false

agent:
  propagation: ts1
  num_trajectories_per_env: 128