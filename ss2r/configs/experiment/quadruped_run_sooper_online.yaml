# @package _global_
defaults:
  - mujoco_playground_dm_control_sooper
  - override /environment: quadruped
  - override /agent: mbpo
  - override /agent/data_collection: step
  - override /agent/cost_robustness: pessimistic_cost_update
  - _self_


training:
  wandb_id: fmc7c57s

agent:
  activation: swish
  reset_on_eval: false
  model_grad_updates_per_step: 50
  critic_grad_updates_per_step: 20
  reward_scaling: 10.
  load_auxiliaries: true